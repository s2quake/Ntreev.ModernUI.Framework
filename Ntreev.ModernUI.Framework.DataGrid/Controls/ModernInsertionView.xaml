<UserControl x:Class="Ntreev.ModernUI.Framework.DataGrid.Controls.ModernInsertionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xcdg="http://schemas.xceed.com/wpf/xaml/datagrid"
             xmlns:nmfc="clr-namespace:Ntreev.ModernUI.Framework.Controls;assembly=Ntreev.ModernUI.Framework"
             xmlns:nmgc="clr-namespace:Ntreev.ModernUI.Framework.DataGrid.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <xcdg:DataGridCollectionViewSource x:Key="source" Source="{Binding ItemsSource}" />
    </UserControl.Resources>

    <StackPanel>
        <nmfc:PropertyItemsControl ItemsSource="{Binding ItemsSource}" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <!--<Button x:Name="Close" Content="{x:Static p:Resources.Button_Close}" IsDefault="True" Style="{DynamicResource Dialog_Button_Style}" />-->
        </StackPanel>
    </StackPanel>

</UserControl>
