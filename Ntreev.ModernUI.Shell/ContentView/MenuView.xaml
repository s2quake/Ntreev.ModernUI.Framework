<UserControl x:Class="Ntreev.ModernUI.Shell.ContentView.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:nmfc="clr-namespace:Ntreev.ModernUI.Framework.Controls;assembly=Ntreev.ModernUI.Framework"
             xmlns:nmfi="clr-namespace:Ntreev.ModernUI.Shell.MenuItems"
             xmlns:shep="clr-namespace:Ntreev.ModernUI.Shell.Properties"
             mc:Ignorable="d" d:DesignWidth="890">

    <nmfc:ModernMenu ItemsSource="{Binding MenuItems}">
        <nmfc:ModernMenuItem DataType="{x:Type nmfi:MessageBoxMenuItem}" Header="{x:Static shep:Resources.MenuItem_File}" />
        <nmfc:ModernMenuItem DataType="{x:Type nmfi:ViewMenuItem}" Header="{x:Static shep:Resources.MenuItem_View}" />
        <nmfc:ModernMenuItem DataType="{x:Type nmfi:ToolMenuItem}" Header="{x:Static shep:Resources.MenuItem_Tool}" />
        <nmfc:ModernMenuItem DataType="{x:Type nmfi:HelpMenuItem}" Header="{x:Static shep:Resources.MenuItem_Help}" />
    </nmfc:ModernMenu>
    
</UserControl>
